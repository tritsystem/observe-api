# Pinned to the exact versions actually running in production (checked via
# `pip show` against the live server) -- was previously >=, meaning a new
# upstream release could silently change production behavior on the next
# fresh install/deploy with no warning. Bump deliberately, not by accident.
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic[email]==2.13.4
stripe==15.4.0
sentence-transformers==5.6.1
faiss-cpu==1.15.0
torch==2.13.0
numpy==2.3.5
rank-bm25==0.2.2
